<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Chainway's DeviceAPI library asks for the camera, for scanning features
         this app does not use (barcodes arrive as keystrokes from the scanner).
         Declared but never granted, it makes Android refuse to open the camera
         app for search by photo - "Permission Denial ... revoked permission
         CAMERA", which closed the app on the C72. The camera app takes the
         photo itself, so this app needs no camera permission at all. -->
    <uses-permission android:name="android.permission.CAMERA" tools:node="remove" />

    <!-- The reader module is a serial device, not a USB accessory, so nothing
         is declared for it. Chainway's SDK opens /dev/ttyMT1 itself. -->

    <application
        android:name=".HandheldApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Handheld"
        android:usesCleartextTraffic="true">

        <!-- usesCleartextTraffic because the warehouse PC is on the LAN over
             plain HTTP. Android 9 and later block cleartext by default, and
             the failure is a connection error with no explanation. -->

        <!-- Search by photo: the camera app is handed a file of ours to write
             the picture into, and it can only reach it through this. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.photos"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/photo_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Handheld">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
